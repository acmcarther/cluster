apiVersion: v1
kind: Secret
metadata:
  name: minio-tiny-user
  labels:
    app: minio-tiny
type: Opaque
data:
  accesskey: {{ .Values.accessKey | b64enc }}
  secretkey: {{ .Values.secretKey | b64enc }}
